# Base URL of the receipt analysis backend.
# Leave empty to use the same-origin development stub in app/api/approval-request/.
# Set it to the real service (no trailing slash) once that is running, e.g.:
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=
